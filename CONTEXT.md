# 模板生产领域模型

本上下文定义从网图重构模板图、编译模板数据和处理用户编辑时使用的统一领域语言。

## Language

**主生产流程（Main Production Flow）**：
从来源网图开始，以正式模板记录落盘结束的完整生产生命周期，固定包含 P0–P8。
_Avoid_: 整个 Skill、模板测试流程、T1

**用户大阶段（Major Production Stage）**：
把 P0–P8 聚合为换图执行、API 模板图生成与确认、模板数据编译、OSS 最终化四个可独立调用且可恢复的操作边界；所有大阶段继续属于同一个生产项谱系。
_Avoid_: 四个独立 Skill、四份互不相干的生产任务

**生产项（Production Item）**：
一张来源网图及其独立的分析、替换决定、版本依据和正式模板记录，是主生产流程的最小单位。
_Avoid_: 整个批量、批次共享结果

**批量提交（Bulk Submission）**：
一次提交多个相互独立生产项的入口；默认不产生共享策略、组合分配、版本依据或汇总产物。
_Avoid_: 共享批次、统一生产任务

**共享批次策略（Shared Batch Policy）**：
用户明确施加给多个生产项的共同要求或跨图分配规则，每张图仍然独立分析与产出。
_Avoid_: 批量提交的默认行为、单图策略

**独立测试调用（Independent Test Invocation）**：
用户明确指定一份现成正式模板记录后发起的效果验证，与主生产流程拥有彼此独立的生命周期。
_Avoid_: P9、生产阶段、上线门禁

**来源网图（Source Web Image）**：
进入生产流程的原始网络图片，提供重构机制、组件关系和来源风险证据。
_Avoid_: 模板图、参考图

**确认模板图（Approved Template Image）**：
第一阶段换图完成并满足确认门禁的默认成图，是标题、描述、槽位默认值、reference 和 cover 的视觉事实源；门禁明确时由 Skill 确认，风险或歧义进入人工决定。
_Avoid_: 候选图、来源图

**有效换图（Meaningful Replacement）**：
在保留来源网图画风与核心机制的前提下，至少替换一个主要画面组件并处理其依赖内容。
_Avoid_: 仅去水印、仅提高清晰度、仅清理平台标记

**主要替换目标（Primary Replacement Target）**：
一次自主换图中被主动改变的核心画面组件，默认每个生产项只有一个。
_Avoid_: 关联项、清洁项、槽位

**替换依赖闭包（Replacement Dependency Closure）**：
主要替换目标变化后，为消除旧身份或语义冲突而必须同步变化的文字、标识、服装特征、影子、倒影、重复实例和关联区域。
_Avoid_: 多个主要替换目标、可选装饰变化

**身份替换群组（Identity Replacement Cohort）**：
一次身份换图中必须同步变化的全部可见身份单元及其专属组件；CP、搭档、固定组合和同图多主体均在该群组中整体替换。
_Avoid_: 只换 CP 中一人、保留第二主体来源身份

**画风保真（Style Fidelity）**：
换图前后保持一致的媒介、构图机制和主要视觉语言；清晰度修复、媒介统一与局部残留清理仍属于保真范围。
_Avoid_: 自主改风格、视觉重设计

**来源目标画布（Source Canvas Decision）**：
P1 在分析替换目标前，对独立设计、商品载体印花、截图内容或完整场景做出的唯一路由；它明确最终成图应保留的区域、要排除的衣服/模特/黑框/设备界面，以及正视化或裁框动作。
_Avoid_: 默认把整件 T 恤当模板、把黑色截屏框锁进构图

**来源标记策略（Source Mark Policy）**：
P1 对水印、平台标、贴纸、装饰图标、商标、无关 Logo 和身份标记逐项作出的保留、同步或删除决定；P2 按同一清单验收。
_Avoid_: 把所有贴纸和商标视为污染、只用一个全画布清洁布尔值

**替换计划（Replacement Plan）**：
描述来源组件要发生的主动变化、依赖变化、保持内容和审核风险的一次生产决策。
_Avoid_: 槽位修改、用户编辑

**替换值池（Replacement Pool）**：
第一阶段为生成确认模板图而评估和选择的替换值集合，只服务来源组件重构。
_Avoid_: 槽位推荐项、用户候选值

**槽位推荐池（Slot Suggestion Pool）**：
模板图确认后，围绕高价值槽位生成的用户可见推荐值集合，只服务模板编辑。
_Avoid_: 第一阶段替换值、模板图候选池

**身份资产（Identity Asset）**：
为提高身份稳定性而额外提供的身份参考图片，是生成辅助来源之一。
_Avoid_: 唯一替换池、正式槽位推荐项

**生成身份（Generated Identity）**：
图片模型根据同类语义生成的新人物或角色身份，不依赖固定本地身份图片。
_Avoid_: 临时资产、来源身份

**高价值槽位（High-value Slot）**：
用户有明确修改动机、变化清晰可见、能稳定生成并保持模板核心机制的开放内容。
_Avoid_: 可检测元素、装饰参数

**模板身份（Template Identity）**：
用户替换所有开放默认内容后仍需成立的动作、关系、容器、空间骨架、阅读顺序、媒介和视觉钩子，是模板玩法的稳定边界。
_Avoid_: 当前默认主体、开放颜色、开放文字、来源图身份

**编辑权限裁决（Edit Authority Decision）**：
针对当前确认模板图，把显著内容分别归入结构化槽位、全文自由编辑、固定视觉内容、清理或人工复核的一次逐图决定。
_Avoid_: 按批次统一套槽、把所有可见内容都开放

**单图事实域（Per-item Fact Domain）**：
由当前 Production Item 的确认模板图摘要、可见事实、组件图、文字区域、关系和用户单图要求组成的作者事实边界。
_Avoid_: 兄弟项标题、上一张图的构图、隐式批次主题

**槽位作者合同（Slot Authoring Contract）**：
把当前图中的高价值目标编写为稳定 ID、类型、标签、默认值、推荐项、图片语义、上传提示和运行绑定的字段职责集合。
_Avoid_: 只满足 JSON Schema 的通用占位文案

**身份特征继承裁决（Identity Feature Inheritance Decision）**：
针对一个身份图片输入，记录图片模式下从用户上传图读取的可辨认特征，以及因参与模板核心玩法而沿用模板值的最小例外。
_Avoid_: 画风合同、槽位价值评估、image.promptValue

**槽位预算（Slot Budget）**：
正式模板允许承载的高价值编辑控件范围，常态为 2–5 个且通常约 3 个；穷尽分析后确实只有一个高价值槽位时允许 1 个。
_Avoid_: 可见组件数量、独立素材数量

**模板标题（Template Title）**：
使用画面机制、动作、关系、容器、视觉钩子或场景描述模板的中性用户可见名称。
_Avoid_: 生成结果说明、专题运营标题

**结构化编辑内容（Structured Editable Content）**：
被选为高价值槽位、通过 `inputSchema` 暴露给用户并支持推荐项替换的内容。
_Avoid_: Prompt Template 中全部可编辑文字、固定呈现维度

**自由编辑内容（Free-editable Narrative Content）**：
没有被选为结构化槽位，但仍具有编辑价值、因此保留在 Prompt Template 自然语言中的画面内容；用户可在全文编辑模式直接改写。
_Avoid_: 无编辑价值的装饰细节、隐藏生成约束

**提示词模板（Prompt Template）**：
用户可见、可全文编辑的完整自然语言画面描述；包含所有结构化编辑内容，也包含适合自由编辑的非槽位内容，最终字面就是用户认为的生图提示词。
_Avoid_: 只由槽位拼出的短句、隐藏 instruction、完整视觉合同

**运行语义（Runtime Semantics）**：
正式模板中描述开放输入接管哪些可见目标，以及跨用户输入必须保持哪些视觉事实的唯一运行合同；固定包含版本、目标实例、输入绑定和视觉合同。
_Avoid_: promptEnhancement、metadata 留档、生产分析全集

**目标实例（Target Instance）**：
确认模板图中可由用户输入接管，或需要明确建立身份边界的具名可见位置，使用稳定 ID、类型、角色和空间区域定位。
_Avoid_: 来源图组件、槽位本身、泛化的整张画面

**输入绑定（Input Binding）**：
把一个 `inputSchema.slots` 输入映射到一个或一组类型匹配目标的运行规则；固定身份输入执行 `one_to_one` 或 `same_source_repeated`，动态合照执行 `preserve_group`，内容输入执行整体替换或保持目标组结构。
_Avoid_: UI 提示、推荐项、生成任务实例

**动态身份群组（Dynamic Identity Group）**：
由一张合照中的全部同类成员整体接管的可伸缩身份区域；核心玩法和高价值槽位两层证据通过后，正式结构为纯图片输入与 `identity_group + preserve_group + group_photo`，人数跟随来源合照并满足作者上下界 `minMembers < maxMembers`。
_Avoid_: 固定 CP 位、固定人数角色位、逐成员可寻址玩法、十二只猫等无身份要求的密集内容、合照选单人、人宠混合自动拆组

**群体策略裁决（Group Strategy Decision）**：
对多主体画面先判断核心玩法需要整组身份、独立身份还是描述性内容，再在高价值槽位挖掘阶段复核最合适的用户输入单位；两层证据共同决定动态合照、独立 subject、同源重复或文字内容组路由。
_Avoid_: 按检测数量随机建槽、把所有多人图都设为群体槽、给每个密集同类对象建立图片槽

**服装归属（Clothing Ownership）**：
每个身份绑定对服装事实来源的显式二态裁决；`source` 表示服装随身份图替换，`template` 表示服装保持模板设定。更细的动作或结构例外继续保存在关系合同中。
_Avoid_: 从旧散文静默猜测、同时省略字段和裁决

**图片来源隔离（Image Source Isolation）**：
身份图只提供身份线索，内容图是它所绑定内容目标的唯一题材来源；身份图背景、构图、色彩和光影不流入内容目标。
_Avoid_: 把身份照的环境当成场景参考

**容器依赖（Container Dependency）**：
对每个图片模式内容目标裁决 `post_edit`、`template_fixed` 或 `independent`；只有内容位于会被重绘目标的封闭轮廓内时使用 `post_edit`。
_Avoid_: 把接触、遮挡或前后层次误判为容器

**视觉运行合同（Visual Contract）**：
从确认模板图提炼的媒介、画风特征、构图、关系和条件性色光事实，约束替换后仍需成立的视觉结构，同时服从开放槽的用户内容权限。
_Avoid_: 具体默认身份、开放槽默认值、负向清除脚手架

**渲染一致性裁决（Rendering Coherence Decision）**：
依据当前确认模板图，把全部可见组件划入统一或刻意混合的渲染单元，并逐个声明 subject 的完整重绘范围与身份权限；它是正式视觉合同中媒介和画风字段的生产作者权威。
_Avoid_: T1 效果结论、批次共享画风、生成后为意外媒介割裂补写理由

**文字区域（Text Region）**：
画面中具有独立空间、语义或视觉角色的一组文字。
_Avoid_: 文字槽

**关键词文字槽（Text Span Slot）**：
从长文本中抽取、具有独立语义和清晰编辑价值的关键词或短语。
_Avoid_: 整段文字槽、全文复制

**固定视觉文字（Locked Visual Text）**：
保留版面、语境或视觉完整性且缺少独立编辑价值的画内文字。
_Avoid_: 遗漏文字、默认文字槽

**身份文字依赖（Identity Text Dependency）**：
主体身份与姓名、英文名、团名、称谓、号码、识别色或身份徽标之间的语义指代关系。
_Avoid_: 自动联动能力、普通文字邻接

**画面硬失败（Visual Hard Failure）**：
旧身份残留、水印残留、身份文字冲突、实例漏换、结构破坏或媒介明显漂移等可观察的画面合同违约，必须修复后才能成为确认模板图。
_Avoid_: 可人工放行的主观偏好、方案选择

**正式模板记录（Gallery Template Record）**：
完成模板图 URL 回填并通过正式 Schema 和业务质量检查的单份模板 JSON，是新 Skill 的唯一正式交付物。
_Avoid_: 生产 sidecar、审核记录、运行测试报告

**正式投影（Formal Projection）**：
从完整生产中间模型编译正式模板记录的白名单过程，只保留运行、展示和正式合同需要的字段。
_Avoid_: 删除生产证据、把 sidecar 整包塞入 metadata

**生产旁证（Production Sidecar）**：
保存替换理由、候选策划、六维视觉事实、版本能力、推荐项理由和审核证据的内部产物，不进入正式模板记录。
_Avoid_: 正式业务 JSON、用户可见字段

**关键结果资格（Critical Outcome Qualification）**：
从已持久化的第一阶段、图像审核、作者审核和四层验证证据重算换图、肢体、Prompt、槽位、视觉合同和标签是否全部合格的闭环账本。
_Avoid_: 文档声明、人工勾选、只看历史完成状态

**生产谱系（Production Lineage）**：
一个生产项由版本 pin、不可变 revision、产物摘要、依赖失效和外部执行凭证构成的可恢复历史。
_Avoid_: 可覆盖目录、对话日志、批次共享版本

**正式合同快照（Gallery Contract Snapshot）**：
生产时采用的上游正式模板合同只读副本，具有来源、摘要、版本和兼容范围。
_Avoid_: 后端实现推测、静默覆盖的当前文件

**模板 JSON 测试（Template JSON Test）**：
使用现成正式模板记录与真实用户上传图模拟槽位编辑或全文编辑，编译后调用 Codex 内置生图工具，并通过真实生成结果判断模板效果的一次独立验证。
_Avoid_: P0–P8 完成门禁、正式模板字段、自动批次测试
