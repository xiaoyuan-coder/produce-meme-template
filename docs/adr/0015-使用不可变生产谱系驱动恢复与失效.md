# 使用不可变生产谱系驱动恢复与失效

每个 Production Item 独立保存 Production Pin、不可变 revision、产物摘要、依赖图、审核证据和外部执行凭证。上游摘要变化时精确失效下游，request ID WAL 与 Asset Receipt 支持幂等恢复；运行中的项目不会因安装版本变化而静默换规则。该设计以额外旁证换取可审计恢复，避免覆盖式目录和“读取最新文件”造成版本错配。
