# 分离模板生产与模板效果测试

模板生产采用 P0–P8 单一主流程，以正式模板 JSON 落盘结束。T1 保留为同一 Skill 提供的独立调用能力，只读取用户明确指定的现成 JSON 和真实用户上传图，编译执行包后调用 Codex 内置生图工具；它拥有独立生命周期，不调用 Fal/OSS，也不参与生产状态、批次完成条件或正式交付。供应商队列与 WAL 回放仅服务发布验证，使用独立名称。这样既保留模板效果验证能力，也避免测试成本和测试结果改变批量生产语义。
