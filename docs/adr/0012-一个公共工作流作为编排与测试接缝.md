# 一个公共工作流作为编排与测试接缝

单图、批量和 T1 通过同一个顶层入口选择各自生命周期，正式生产都进入同一公共工作流；批量只拆分 Production Item，T1 使用独立子命令。生图、轮询、视觉证据和 OSS 通过注入式 adapter 接入，这个公共接口也是最高测试 seam。该选择保留一个稳定用户结果，同时让外部动作可替换、可测试，避免内部模块重新演化为多个相互竞争的业务入口。
