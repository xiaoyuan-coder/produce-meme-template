# 分离三类版本并使用不可变发布包

Skill 行为版本、内部 Artifact Schema 版本和上游 Gallery Contract 版本独立演进；`release.json` 是行为版本的唯一人工事实源，上游合同使用带摘要的只读快照。发布包冻结全部文件摘要，安装副本只来自验证后的发布包，每个 Production Item 再独立 pin release 与合同。该设计增加发布步骤，换取源码、安装副本、生产项和正式合同之间可证明的一致性。
