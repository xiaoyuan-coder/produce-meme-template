# 自主换图使用单一主要目标与依赖闭包

Skill 自主策划时，每个生产项默认只选择一个主要替换群组，同时同步替换群组内的全部可见身份主体，以及它们的专属配饰、关联文字、标识、服装身份特征、影子、倒影、重复实例和关联区域。CP、搭档、固定组合与同图多主体都属于同一身份替换群组；任一成员不能单独保留来源身份。

群组内关联项属于替换依赖闭包，不计为额外主要目标。只有同时主动改变两个彼此无身份关系、无依赖关系的内容轴，才算多目标主动替换。这类额外改动只响应用户明确要求，或用于清除无法通过单个替换群组解决的来源风险。
